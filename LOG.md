# 更新日志

## v26.6.7

### 新增
- 播放器重做为 Apple Music 风格全屏界面：重构封面页、歌词页、控制区显隐与小屏设备方向限制，并隐藏播放页状态栏。
- 新增基于专辑封面取色的流体背景，Android 13+ 使用 AGSL RuntimeShader 渲染，低版本保留兼容背景。
- 引入 San Francisco 字体资源并设为全局字体，补齐多语种 SF/Noto 字体映射。
- 封面页歌曲名过长时支持跑马灯显示。

### 优化
- 1:1 贴近 music-lyric-player-web 重做歌词逐字动画，优化 Apple Music 风格高亮、重音闪烁、翻译/音译布局与显隐逻辑。
- 翻译与音译切换改为互斥，音译恢复到正文下方，并支持逐字音译高亮。
- 优化播放器按钮、歌词点击反馈、圆角、颜色、间距和小屏显示，移除长按复制歌词与顶部返回按钮。
- 重新整理封面取色和背景亮度，取消自定义背景图对播放器背景的干扰。

### 修复
- 修复歌词逐字动画概率出现整行先高亮再逐字播放的问题。
- 修复同步在线状态下本地歌单变更不上报的问题：实时 action 改为官方 `list_data_overwrite`，并在初始同步完成后再 flush 待推送变更。
- 修复同步握手阶段提前标记 md5 导致本地改动被误判为已同步的问题。
- 修复普通 UI 加歌、删歌、排序、新建歌单并添加歌曲时未 bump 歌单 revision，导致 watcher 不触发的问题。
- 修复 LX 合并模式下整包数据误删本地歌曲、酷狗歌曲 key 撞号、非 LX 支持平台发送空歌曲项等同步边界问题。
- 修复 Android 13 RuntimeShader 颜色 uniform 使用错误导致的崩溃。

## v26.6.6

### 新增
- 整专下载支持单独保存一份专辑封面文件（命名 cover，按图片实际类型保存为 cover.jpg / cover.png），每个专辑文件夹仅保存一次，可在设置中开关。
- 整专下载支持在文件名追加采样率/位深标记（形如 `[16Bit-44.1kHz]`，仅无损 FLAC 生效，从下载完成的文件头读取），可在设置中开关。
- 媒体通知/小米灵动岛新增「收藏」「桌面歌词」两个功能键（通过 PlaybackState CustomAction 实现，使用官方切图）：点击收藏切换收藏状态、点击歌词切换桌面歌词显隐，图标随状态变化；桌面歌词无悬浮窗权限时引导授权。

### 优化
- 统一设置页与下载页的视觉风格：下载任务卡片对齐歌单卡片基准（间距/圆角/阴影/主题色），合并重复的分类卡片；设置页消除多处手写的列表行、统一为通用组件，统一对话框、分组标题、卡片背景与按钮样式。

## v26.6.4

### 修复
- 修复 QQ 音乐「猜你喜欢」「专属好歌」两个推荐入口的歌曲信息不完整、无法播放的问题：推荐流卡片只携带 songId 与运营封面，缺少 mid/mediaMid/音质信息，现改为用 songId 批量补全完整歌曲详情后再展示。
- 修复三个推荐入口（每日30首、猜你喜欢、专属好歌）封面写死、与实际内容不符的问题：改为从推荐流接口动态获取真实封面（每日30首取真实歌单封面，另两个取推荐流首歌的专辑封面），取不到时回退到默认运营图。

### 优化
- 将三个推荐入口的标题、描述、数量、兜底封面收敛到统一定义，消除 getUserPlaylist 与 getPlayListInfo 两处的重复硬编码。
